<h1>All tasks</h1>
<ul>
  <% @tasks.each do |task| %>
    <li> <%= check_box_tag "task_#{task.id}_completed", task.id, task.completed %> <%= link_to task.title, task_path(task)  %> </li>
  <% end %>
</ul>
